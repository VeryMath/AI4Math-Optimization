# OpenCode Loading Notes

This repository exposes a shared Skill layer at:

```text
cdopt-optimization/SKILL.md
```

OpenCode can use the repository checkout directly by reading `AGENTS.md`,
`SKILL.md`, and the concrete Skill file. Keep any OpenCode wrapper thin and
point it back to the shared Skill layer.
